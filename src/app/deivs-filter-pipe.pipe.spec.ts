import { DeivsFilterPipePipe } from './deivs-filter-pipe.pipe';

describe('DeivsFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DeivsFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
